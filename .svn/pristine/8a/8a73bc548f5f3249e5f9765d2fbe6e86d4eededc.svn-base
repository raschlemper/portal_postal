app.directive('trimApp', function () {
    return {
        restrict: 'A',
        scope: {
            ngModel: '='
        },
        link: function(scope, element, attrs) {
//            element.css("font-weight", "bold");
//            scope.$watch('ngModel', function(newValue, oldValue) {
//                console.log(newValue, oldValue, attrs.label);
//                newValue.descricao = newValue.descricao.replace(/^\s+|\s+$/g, '');
//                element.val(newValue.descricao);
//                element.find('option:selected').text(descricao);
//                console.log(scope, element, attrs);
//                var descricao = element.find('option:selected').text();
//                descricao = descricao.replace(/^\s+|\s+$/g, '');
//                element.find('option:selected').text(descricao);
//            });
//        if(!angular.isString(lancamento.planoConta.descricao)) {
//            return lancamento;
//        }  
//        lancamento.planoConta.descricao = lancamento.planoConta.descricao.replace(/^\s+|\s+$/g, '');
        }
    };
});
